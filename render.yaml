services:
  - type: web
    name: my-api
    env: node
    region: oregon
    plan: free
    branch: main
    repo: .
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    env:
      PORT: "3000"
      NODE_ENV: "production"
      DEBUG: "false"
